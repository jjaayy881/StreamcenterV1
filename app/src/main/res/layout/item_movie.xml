<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:background="@drawable/focus_background"
    android:clickable="true"
    android:focusable="true"
    android:padding="10dp"
    android:layout_marginBottom="9dp">

    <!-- Poster: feste 2:3-Groesse, damit die Liste nicht je nach Bildformat
         hin- und herspringt. Wird per Glide befuellt, bleibt bei fehlendem
         poster_url einfach unsichtbar (kein Platzhalter-Icon noetig auf TV-Distanz). -->
    <ImageView
        android:id="@+id/image_poster"
        android:layout_width="60dp"
        android:layout_height="90dp"
        android:layout_marginEnd="14dp"
        android:scaleType="centerCrop"
        android:background="@color/color_surface"
        android:visibility="gone" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_gravity="center_vertical"
        android:orientation="vertical">

        <TextView
            android:id="@+id/text_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="@color/color_text_primary"
            android:textSize="16sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/text_overview"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:textColor="@color/color_text_secondary"
            android:textSize="13sp"
            android:maxLines="2"
            android:ellipsize="end"
            android:visibility="gone" />
    </LinearLayout>
</LinearLayout>
